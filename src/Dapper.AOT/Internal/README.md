Note; these files aren't actually `internal`, because the generated code needs to access them - however, they should not be
used from user code. As such, they are marked `[Obsolete]` to discourage usage.